import theme from '../../../../../theme';

const ProfileNameAtom = {
  fontWeight: 'bold',
  color: theme.primary[500],
  whiteSpace: 'nowrap',
  overflow: 'hidden',
  textOverflow: 'ellipsis',
};

export default ProfileNameAtom;
