import theme from '../../../../../theme';

const ProfileLogoutAtom = {
  cursor: 'pointer',
  color: theme.primary[500],
  '&:hover': {
    backgroundColor: theme.primary[100],
  },
};

export default ProfileLogoutAtom;
