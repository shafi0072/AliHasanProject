import theme from '../../../../../theme';

const TabButtonAtom = {
  fontSize: '16px',
  fontWeight: 'bold',
  color: `${theme.common[50]} !important`,
  textTransform: 'none',
};

export default TabButtonAtom;
